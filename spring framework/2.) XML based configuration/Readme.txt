Pom.xml (Product object Model) is a xml file which is used to import dependencies as well as contains 
information details used by Maven to build the project.

Maven: It is a powerful project management tool based on POM used for projects build, 
dependency and documentation.

Spring.xml contains information for various veans which are implemented in the
main java class.

Staff is an interface which binds the methods of both Dsoctor and Nurse classes.

Then, examples demonstrate the dependency injection using setter as well as constructor.